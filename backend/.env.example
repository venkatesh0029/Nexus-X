# Server
HOST=127.0.0.1
PORT=8000
WEBSOCKET_PORT=8000

# LLM Providers (Local by default, no API keys needed for LM Studio / llama.cpp)
# Set these if using external providers or non-default local ports
LM_STUDIO_URL=http://localhost:1234/v1
LLAMACPP_URL=http://localhost:8080/v1
OPENAI_API_KEY=

# Optional API Keys for other tools (e.g. search)
TAVILY_API_KEY=
